import type { Explanation } from "./explanation-types";
import type { QuizSet } from "./quiz-types";

/**
 * Source-grounded explanations + quizzes for Cybersicherheit SoSe 2025.
 *
 * Intentionally empty. Each lecture's explanations and quizzes are written
 * from the corresponding `*.md` next to its PDF — never from prior knowledge.
 * See `AGENTS.md` for the pipeline.
 */
export function buildCybersicherheit2025Explanations(): Explanation[] {
  return [];
}

export function buildCybersicherheit2025QuizSets(): QuizSet[] {
  return [];
}
