"use client";

/**
 * Previously the floating PDF-zoom + Walkthrough/Quiz pill. Chapter view
 * exposes its own controls inline and PDFs flow at their natural column
 * width, so this is intentionally a no-op.
 */
export function LessonNavBar() {
  return null;
}
